# js-age-verifier
An age verification web app utilizing JavaScript conditional statements. The program takes a user's age input and evaluates it using if / else logic. The project highlights dynamic HTML content modification via innerHTML to display 'Adult' or 'Minor' based on the result, all wrapped in a clean and styled CSS user interface.
